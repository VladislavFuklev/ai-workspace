{
  "name": "ai-workspace",
  "version": "0.0.0",
  "private": true,
  "description": "AI Workspace — multi-tenant AI document intelligence platform",
  "packageManager": "pnpm@11.4.0",
  "engines": {
    "node": ">=22.0.0 <23.0.0",
    "pnpm": ">=11.0.0"
  },
  "scripts": {
    "dev": "pnpm --filter @ai-workspace/web dev",
    "build": "pnpm --filter @ai-workspace/web build",
    "start": "pnpm --filter @ai-workspace/web start",
    "typecheck": "pnpm --filter @ai-workspace/web typecheck"
  }
}
