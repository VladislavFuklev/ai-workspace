# Task N.N — Title

## Objective

One sentence: what is true after this task that is not true now.

## Context

What already exists that this builds on, and the constraint that makes this
non-obvious. Link the relevant ADR or architecture section.

## Prerequisites

Which tasks must be done, and what must be installed.

## Implementation steps

1. Numbered, each a concrete change. Name files where they are known.
2. Note explicitly what belongs to a later task and is therefore excluded.

## Acceptance criteria

- Each one independently checkable by running something.
- Written so "did you test it" has a yes/no answer, not an opinion.

## Tests

Which level (see the `testing` skill), or "verification is X" when no automated
test applies.

## Documentation

Which files to update. An ADR only if a decision constrains future work.

## Commit

`<scope>: <imperative summary>`

---

<!-- Filled in when the task is done. Budget ~20 lines; see the project-manager
     skill. Delete the sections that are empty rather than writing "none". -->

## Outcome — completed YYYY-MM-DD

One or two sentences on what shipped, and the versions chosen if any.

| Check | Result |
| --- | --- |
| | |

### Deviations
Where the work departed from the plan, and why. This is the highest-value part:
a future session that cannot see the reasoning will undo it.

### Known limitations
What is knowingly incomplete, and which task addresses it.

### Next task
N.N — Title.
