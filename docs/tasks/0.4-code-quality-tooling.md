# Task 0.4 — Code quality tooling

## Objective

One command per workspace that formats, lints and type-checks, with identical
results on a contributor's machine and in CI.

## Context

Task 0.2 left ESLint installed by the Next.js scaffolder but unconfigured beyond
its default, and the Python side has no linter, formatter or type checker at all.
Task 0.6 will run whatever this task defines, so the commands must be
non-interactive and exit non-zero on failure.

## Prerequisites

- Tasks 0.2 and 0.3 complete.

## Implementation steps

1. **Web** — add Prettier (with the Tailwind class-sorting plugin), reconcile it
   with the existing `eslint.config.mjs`, and add rules that enforce the layering
   in `docs/ARCHITECTURE.md` (no cross-feature internal imports, no import from
   `app/` into `features/`) using an import-boundary rule.
2. **API** — add Ruff (lint + format) and a type checker, configured in
   `apps/api/pyproject.toml`. Enable rule sets deliberately rather than everything;
   record which and why.
3. Decide the TypeScript version question deferred in 0.2: the scaffold pins `^5`
   while TypeScript 7 exists. Check whether `eslint-config-next` and the Next.js
   plugin support 7 before moving, and record the outcome.
4. Add root scripts so one command checks everything, and a `--fix`/`--write`
   variant that is never run implicitly by the check command.
5. Add `.editorconfig`.
6. Consider a pre-commit hook. Only if it stays fast; a slow hook gets bypassed.

## Acceptance criteria

- A single documented command reports lint, format and type status for both apps.
- Every check exits non-zero on a real violation, verified by introducing one.
- Formatting is deterministic: running the formatter twice changes nothing.
- The checks pass on the current codebase without weakening a rule to achieve it.
- No check requires network access.
- Layering rules actually fail on a deliberate cross-feature import.

## Tests

No unit tests. Verification is running each check against a deliberately broken
file and confirming the failure, then reverting.

## Documentation

Update `docs/ARCHITECTURE.md` (toolchain table), `docs/PROJECT_STATE.md`,
`docs/CHANGELOG.md`, `docs/ROADMAP.md`, this file, and `docs/DECISIONS.md` for the
TypeScript version decision and the Ruff rule-set choice.

## Commit

`chore: add linting, formatting and type checking`

---

## Outcome — completed 2026-09-12

**Status:** Complete.

### Versions selected

| Tool | Version | Note |
| --- | --- | --- |
| Prettier | 3.9.6 | + `prettier-plugin-tailwindcss` 0.8.1, `eslint-config-prettier` 10.1.8 |
| eslint-plugin-boundaries | 7.2.0 | layering enforcement |
| ESLint | 9.39.5 | held back — see ADR-010 |
| TypeScript | 5.x | held back — see ADR-010 |
| Ruff | 0.16.7 | lint + format |
| mypy | 2.3.1 | `strict` |

### Step 3 — the deferred TypeScript question, answered

Read from the npm registry, not assumed:

- `typescript-eslint@8.70.0` peer: `typescript ">=4.8.4 <6.1.0"` → **TypeScript 7
  is not usable** while `eslint-config-next` depends on it.
- npm additionally reports `eslint@9.39.5` as deprecated (`latest` is 10.10.0,
  9.x is `maintenance`). But `eslint-plugin-import`, `eslint-plugin-react`,
  `eslint-plugin-jsx-a11y` and `eslint-plugin-react-hooks` — all dependencies of
  `eslint-config-next@16.3.5` — cap their `eslint` peer at `^9`. **ESLint 10 is not
  usable either.**

Both recorded in ADR-010 with the condition that lifts them.

### Files created
- `apps/web/.prettierrc.json`, `apps/web/.prettierignore`
- `.editorconfig`
- `scripts/check.sh`, `scripts/fix.sh`, `.githooks/pre-commit`
- ADR-010, ADR-011, ADR-012
- `docs/tasks/0.5-environment-configuration.md`

### Files modified
- `apps/web/eslint.config.mjs` — Prettier compatibility layer plus the
  `boundaries/dependencies` layering policy
- `apps/web/package.json`, `package.json` — check/fix scripts
- `apps/api/pyproject.toml` — Ruff and mypy configuration; `uv.lock`
- `scripts/bootstrap.sh` — enables `core.hooksPath`, mentions the new commands
- `docs/ARCHITECTURE.md` — quality-gate section and toolchain rows
- `next.config.ts` — reformatted by Prettier

### Checks run

| Check | Result |
| --- | --- |
| `scripts/check.sh` — all six checks | pass in **2.2s** |
| ruff on a deliberately bad file | 5 violations: `B006`, `T201`, `DTZ005`, `S602`, `PTH109`; exit 1 |
| mypy on an untyped + mistyped file | 2 errors; exit 1 |
| `ruff format --check` on misformatted code | 1 file would be reformatted; exit 1 |
| `prettier --check` on misformatted code | flagged `next.config.ts`; exit 1 |
| Prettier determinism | second `--write` changed 0 files |
| boundaries: legal imports (feature → own internals, components, lib, external) | 0 errors |
| boundaries: feature → sibling feature | error, exit 1 |
| boundaries: component → feature | error, exit 1 |
| boundaries: lib → components | error, exit 1 |
| `scripts/fix.sh` round trip | removed an unused import, reformatted both languages, then reported clean |
| pre-commit hook on a bad commit | commit aborted, HEAD unchanged |
| API image rebuild after the lockfile change | builds; `ruff`, `mypy` and the package all work inside it |
| network | every check runs offline after install |

### Acceptance criteria
- One documented command for both apps — yes, `scripts/check.sh` (`pnpm check`).
- Every check exits non-zero on a real violation — yes, each verified individually
  against a deliberately broken file, then reverted.
- Formatting deterministic — yes, verified by a second run.
- Checks pass on the current codebase without weakening a rule — yes; the only
  change needed was reformatting `next.config.ts`.
- No network access required — yes.
- Layering rules fail on a deliberate cross-feature import — yes, plus two more
  violation shapes and one legal case.

### Deviations from the plan, and why
1. **`boundaries/element-types` → `boundaries/dependencies`.** The first
   implementation used the v5-era rule and syntax; the plugin emitted three
   migration warnings. Rewrote against the v7 `boundaries/dependencies` API from
   the plugin's own README rather than leaving deprecated configuration behind.
2. **Python checks run from `apps/api`, not the repository root.** Running mypy
   from the root with `--config-file` produced `Duplicate module named "__main__"`
   — its `files` entries resolve relative to the working directory. Found by
   running the script, not by reading it.
3. **Pre-commit checks the whole tree, not staged files.** Partially-staged files
   make the linter's view ambiguous, and the full run is ~2s. No `husky` or
   `lint-staged` dependency: a shell script in `.githooks/` plus `core.hooksPath`
   does the same thing without adding a JS dependency for the Python side.
4. **API layering is not linter-enforced.** Ruff can ban relative parent imports
   (enabled) but not "layer A may not import layer B". Adding a bespoke import
   linter with zero Python code in those packages would be speculative; revisit
   when phase 2 puts code in them.

### Known limitations
- ESLint 9 is on the maintenance channel and npm marks it deprecated. Blocked by
  `eslint-config-next`'s plugin set; see ADR-010.
- The API dev image now carries Ruff and mypy, because `uv sync` includes dev
  dependencies. Correct for a dev image; task 13.1 will use `--no-dev`.
- Prettier is scoped to `apps/web`; Markdown under `docs/` is not formatted, to
  avoid reflowing the entire documentation set.
- `mypy` `strict` currently checks 10 near-empty files. Its real cost appears in
  phase 2.
- The pre-commit hook is opt-in per clone (`core.hooksPath` is not committed
  config). `scripts/bootstrap.sh` sets it and says so.

### Next task
0.5 — Environment configuration.
