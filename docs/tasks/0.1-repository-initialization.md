# Task 0.1 — Repository initialization and architecture decision

## Objective

Inspect the repository and establish the initial engineering baseline without prematurely implementing product features.

## Steps

1. Inspect repository contents.
2. Inspect existing git configuration and history.
3. Decide whether the repository should be initialized as a monorepo.
4. Define initial workspace boundaries for web, API and shared tooling.
5. Record architecture decisions.
6. Ensure project documentation exists.
7. Do not implement authentication, database models or AI features yet.

## Acceptance criteria

- Repository structure is documented.
- Monorepo decision is documented.
- Initial architecture is documented.
- Claude Code memory files exist.
- Roadmap exists.
- No unnecessary product code is created.

## Tests

Run only relevant repository/configuration validation.

## Documentation

Update:
- `docs/PROJECT_STATE.md`
- `docs/ARCHITECTURE.md`
- `docs/DECISIONS.md`
- `docs/CHANGELOG.md`
- `docs/ROADMAP.md`

## Commit

`chore: initialize project architecture`

---

## Outcome — completed 2026-09-12

**Status:** Complete.

### Decisions recorded
- ADR-001 — single monorepo for web, API and infrastructure
- ADR-002 — workspace boundaries and layered internals
- ADR-003 — pnpm workspaces with Node 22 LTS; no JS task runner yet
- ADR-004 — uv with a pinned Python 3.13 interpreter
- ADR-005 — two independent dependency graphs joined at the HTTP contract

### Files created
- `.gitignore`
- `README.md` (placeholder until task 14.6)
- `docs/tasks/0.2-monorepo-setup.md`

### Files updated
- `docs/ARCHITECTURE.md` — repository topology, per-app internals, toolchain table,
  cross-language boundary
- `docs/DECISIONS.md` — ADR-001 … ADR-005
- `docs/PROJECT_STATE.md`
- `docs/CHANGELOG.md`
- `docs/ROADMAP.md`

### Files removed from version control
- `.claude/.DS_Store`, `docs/.DS_Store` — untracked via `git rm --cached`; they were
  committed before `.gitignore` existed. Still present on disk, now ignored.

### Checks run
- Repository inspection (no source code present before or after).
- Toolchain availability: Node 22.22.3, pnpm 11.4.0, uv 0.12.0, Docker 29.5.2.
  System Python is 3.9.6 and is deliberately not used (see ADR-004).
- Git tree clean before the task; only intended files changed after.
- No build, lint or test suite exists yet, so none was run.

### Acceptance criteria
- Repository structure documented — yes, `docs/ARCHITECTURE.md`.
- Monorepo decision documented — yes, ADR-001.
- Initial architecture documented — yes.
- Claude Code memory files exist — yes (`CLAUDE.md`, `docs/`, `.claude/`).
- Roadmap exists — yes, 0.1 ticked.
- No unnecessary product code created — yes, none.

### Known limitations
- Directory layout is documented but not yet materialized; task 0.2 creates it.
- Python 3.13 is a forward-looking pin with a documented fallback to 3.12.

### Next task
0.2 — Monorepo setup.
