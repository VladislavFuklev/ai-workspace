export default function WorkspacePage() {
  return (
    <div className="mx-auto max-w-5xl px-4 py-8 sm:px-6 lg:px-8">
      <h1 className="text-2xl font-semibold tracking-tight text-text">Workspace</h1>
      <p className="mt-2 max-w-prose text-sm text-text-muted">
        The application frame. Documents, search and the assistant arrive in phases 5 to 7; this
        route exists so they have somewhere to land.
      </p>
    </div>
  );
}
